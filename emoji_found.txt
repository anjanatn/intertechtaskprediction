Line 6227: ['0x2605']
  Context: ${isChamp ? '<span class="champ-badge">Γÿà</span>' : ''}
Line 6356: ['0x2605']
  Context: <div class="ai-model-matrix-row-name">${escapeAIText(name)}${name === meta.champion ? ' Γÿà' : ''}</div>
